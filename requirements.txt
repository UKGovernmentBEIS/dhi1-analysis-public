# NOTE: these are the constraints the analysis was developed against, not a lockfile.
# The exact versions used to produce the committed results in parameter_estimates/ were
# not captured. Before publishing, regenerate this from the environment that produced
# them:  pip freeze > requirements.txt
# numpyro/jax in particular can shift MCMC results across minor versions.

numpy>=1.20.0
pandas>=1.3.0
numpyro>=0.10.0
jax>=0.3.0
jaxlib>=0.3.0
arviz>=0.12.0
seaborn==0.13.2
matplotlib==3.10.1
statsmodels

# Required by setup.sh, which registers the Jupyter kernel.
ipykernel
